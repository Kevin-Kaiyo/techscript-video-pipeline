# Legacy EP01 Static-Image Pipeline

This directory preserves the first Micro LED EP01 prototype scripts.

These scripts target the old `assets/` and root `output/` layout and are not part of the current reusable TechScript workflow. Keep them as historical reference only.

Current workflow lives in:

- `episodes/<ep>/script.md`
- `episodes/<ep>/animations/hyperframes/index.html`
- `pipeline/tts_cli.py`
- `pipeline/auto_schedule.mjs`
- `pipeline/build_episode.sh`
